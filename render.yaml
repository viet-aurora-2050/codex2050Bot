
services:
  - type: web
    name: codex2050-render-final
    env: python
    plan: starter  # free tier sollte reichen
    region: frankfurt
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
